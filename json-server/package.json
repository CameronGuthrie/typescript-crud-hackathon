{
  "name": "json-server",
  "version": "1.0.0",
  "main": "*.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.3",
    "typescript-crud-hackathon": "file:.."
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
